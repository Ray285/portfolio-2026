import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Raymond Lemon | Portfolio 2026",
  description:
    "Draggable 3D desk, soft shadows, and a white flat-lay portfolio.",
};

/**
 * Home route: the visible experience is the full-screen canvas from
 * `(desk)/layout` → `DeskShellLayout`.
 */
export default function HomePage() {
  return null;
}
