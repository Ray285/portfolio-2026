import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "About | Raymond Lemon",
  description: "About — flat-lay desk view.",
};

/**
 * About shares the same canvas as home; `DeskScene` branches on `/about`.
 */
export default function AboutPage() {
  return null;
}
