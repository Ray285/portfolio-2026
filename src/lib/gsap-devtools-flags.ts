/**
 * Set to `true` to load the official GSAP DevTools **timeline** UI in development
 * (bottom scrubber, play/speed, etc.). Defaults off; intros still run in GSAP.
 */
export const GSAP_DEVTOOLS_ENABLED = true;
